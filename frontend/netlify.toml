[build]
  base = "."
  publish = "build"
  command = "yarn build"

[build.environment]
  NODE_VERSION = "20"

# SPA redirects - ensures React Router works correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

