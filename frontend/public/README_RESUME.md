# Resume Storage

Place your resume PDF file here and name it `resume.pdf`.

Then update `src/mock.js`:
```javascript
resumeUrl: "/resume.pdf"
```

Alternatively, you can:
- Upload to cloud storage (S3, Google Drive, etc.) and use the public URL
- Host on GitHub and use the raw file URL
- Use the current external URL if it's permanent

